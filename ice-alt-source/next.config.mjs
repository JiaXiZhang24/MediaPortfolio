/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/videography-alt-2",
  assetPrefix: "/videography-alt-2",
  images: { unoptimized: true },
};

export default nextConfig;
